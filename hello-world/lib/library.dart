import 'dart:async';
import 'package:flutter/material.dart';

const TextStyle testStyle = TextStyle(
  color: Colors.purple,
  fontSize: 30
);

StreamController<String> textStream = StreamController<String>();